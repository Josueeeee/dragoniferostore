<style>
  /* Estilo general del formulario */
  .contact-form {
    background: linear-gradient(135deg, #2b2b2b, #5f2a6f);
    border: 2px solid #ff00a1;
    box-shadow:
      0 0 25px rgba(255, 0, 161, 0.8),
      inset 0 0 15px rgba(255, 0, 161, 0.5);
    padding: 2rem;
    border-radius: 15px;
    color: #fff;
    font-family: "Orbitron", sans-serif;
    backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;
  }

  .contact-form:before {
    content: "";
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(
      circle,
      rgba(255, 0, 129, 0.15),
      transparent 70%
    );
    animation: pulse 6s infinite ease-in-out;
  }

  @keyframes pulse {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.1);
    }
    100% {
      transform: scale(1);
    }
  }

  .contact-input {
    background-color: #1b1b1b;
    color: #d1d1d1;
    border: 1px solid #ff00a1;
    padding: 0.85rem;
    border-radius: 5px;
    margin-top: 0.75rem;
    transition:
      border-color 0.3s ease,
      background-color 0.3s ease,
      box-shadow 0.3s ease;
    box-shadow: 0 0 5px rgba(255, 0, 161, 0.3);
  }

  .contact-input:focus {
    border-color: #ff00a1;
    background-color: #2b2b2b;
    box-shadow: 0 0 15px rgba(255, 0, 161, 0.7);
    outline: none;
  }

  .contact-label {
    display: block;
    font-weight: 600;
    color: #ff00a1;
    margin-top: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.1rem;
  }

  .contact-button {
    background-color: #ff00a1;
    color: #000;
    padding: 0.85rem;
    border-radius: 50px;
    margin-top: 1.5rem;
    text-align: center;
    display: block;
    transition:
      background-color 0.3s ease,
      transform 0.3s ease,
      box-shadow 0.3s ease;
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: bold;
    letter-spacing: 0.1rem;
    box-shadow: 0 0 15px rgba(255, 0, 161, 0.7);
    position: relative;
    overflow: hidden;
  }

  .contact-button:before {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    width: 300%;
    height: 300%;
    background: radial-gradient(
      circle,
      rgba(255, 0, 161, 0.1),
      transparent 60%
    );
    transform: translate(-50%, -50%) scale(1);
    transition: transform 0.5s ease;
    z-index: 1;
  }

  .contact-button:hover:before {
    transform: translate(-50%, -50%) scale(1.5);
  }

  .contact-button:hover {
    background-color: #e6007f;
    transform: scale(1.1);
    box-shadow: 0 0 25px rgba(255, 0, 161, 1);
  }

  .contact-button:active {
    transform: scale(0.95);
  }

  /* Estilos de la cabecera */
  .contact-form h2 {
    font-size: 2.5rem;
    font-weight: 700;
    text-align: center;
    color: #ff00a1;
    margin-bottom: 1.5rem;
    letter-spacing: 0.2rem;
    text-transform: uppercase;
    text-shadow: 0 0 10px rgba(255, 0, 161, 0.8);
  }
</style>

<div class="contact-form max-w-md mx-auto">
  <h2>Contáctanos</h2>
  <form action="https://formsubmit.co/your-email@example.com" method="POST">
    <label class="contact-label" for="name">Nombre:</label>
    <input class="contact-input" type="text" id="name" name="name" required />

    <label class="contact-label" for="email">Email:</label>
    <input
      class="contact-input"
      type="email"
      id="email"
      name="email"
      required
    />

    <label class="contact-label" for="message">Comentario:</label>
    <textarea
      class="contact-input"
      id="message"
      name="message"
      rows="4"
      required></textarea>

    <button class="contact-button" type="submit">Enviar</button>
  </form>
</div>
